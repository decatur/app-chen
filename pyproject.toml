[tool.poetry]
name = "appchen"
version = "0.3.0"
description = "a client/server web framework based an Python and modern JavaScript."
authors = ["Wolfgang Kühn"]
homepage = "https://github.com/decatur/app-chen"

[tool.poetry.dependencies]
flask = "^1.1"
pymongo
sseclient
requests
